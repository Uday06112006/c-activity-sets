1. What is # in a C language?
-> It is the symbol used primarily for preprocessor directives.
2. what is stdio.h in C language?
-> STandarD Input and  Output
3. what does printf in C language refer to?
-> It is a Standard library function declaration.
4. What does "\n" in C language refer to?
-> It is a new line character.
5. What  does return in C language refer to?
-> It is used to exit a function and optionally return a value to the caller.
6. what does int in C language refer to?
-> int refers to integer in c language.
7. Why is it used for?
-> It is used to declare an integer value.
8. What is main()?
-> It is a function to be executed always.
9. What is the use of main() is C language?
-> The function main() can call other functions.
10. What is ; in c language? 
-> used to end the statement
11. For what statements ";" is used?
-> It is used to end the declaration statements.
12. What is the function of printf in C language?
-> Used to output 
13. What is the function of {} in C language ?
-> The statements enclosed within them is called the body of the function.
14. What is the function of scanf in C language?
-> used to input    
15. What is void in C language?
-> It is keyword used to specify that a function does not return a value.
16. Difference between local, global and parameter.
->Local variables:The variables declared inside a function i.e inside the brackets and they are accessible only within that function and once the function exits, the local variables are destroyed.
->Parameter :  Variables that are passed to the function when they are called i.e (int a), and are accessible only with in that function. so each time these functions are called they are initialised to the values.
-> Global variables : They are declared outside the function and are accessible in the whole program.

SQUARE NUMBER

Step 1:Start 
Step 2:input n

/* square root will be found in n iterations */
/* 
step 3:
root = n/2;
while ( root * root != n)   
  if (root * root < n)
    root = (n + root)/2
  else if (root*root >n)
  root = root/2 
  end if 
  end if
   end while
   output root

github.git